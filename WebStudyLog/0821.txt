1、C/S架构：普通的电脑客户端+服务器，如：LOL；
	B/S架构：网页端+服务器，如：各种页游；

2、
	a）框架开头加分号
	++ --会跟后面的代码合并
	驼峰命名+拼音起名
	js中变量哪怕赋值了也是在使用时才有类型，var局部，不用全局
	5+1种数据类型  数字  字符串  布尔  undefine  null   其他（object）

	只有对象可以调用方法，变量不行，但是变量调用方法不会报错，因为在解析的时候自动	生成一个对象，调用方法之后再释放掉，所以变量调用方法不会报错但不会有想要的结果
		基础数据类型不可修改，typeof查看数据类型返回的都是字符串

3、
	js放到最后面，防止阻塞界面
	通过浏览器设置禁用缓存，js文件缓存加随机号，开发调试使用，避免缓存
	js内部调用快于外部（手机端差别大）
	mime类型 type
	html css不去分大小写，但是js区分
