Web学习

1、在 JS 语言中，一切都是对象。因此，任何支持的类型都可以通过 JSON 来表示，例如字符串、数字、对象、数组等。但是对象和数组是比较特殊且常用的两种类型。
	？“Node.js”、“JSON”？
	
	<script type="text/javascript" src="loadui.js"></script>  
	
1、变量用var做声明，也可以不做声明，其实都差不多，因为var也不会规定数据类型，还是到实际使用时才规定。
	（弱类型语言）

2、字符串可以是双引、单引、混用，真提莫随便啊，注意，引号套用的话，只有外面的引号是用来定义字符串的，里面的就是单纯的字符，会显示出来；而且不能同时使用两次一样的引号，会错乱。

3、有一种数字类型。数字可以带小数点，也可以不带：

4、布尔只能有两个值：true 或 false：

5、数组可以带Array（）关键字，也可以不带，例如：
		a)	var shuzu = new Array();
			shuzu[0]="tom";
			shuzu[1]="tm";
		b)	var shuzu = new Array("tom", "tm");
		c)	vat shuzu = ["tom", "tm"];注意最后一种，不带array关键字，是中括号；
		
6、逻辑运算：&&逻辑与返回true、false；
			 & 按位与返回1,0；没什么差别